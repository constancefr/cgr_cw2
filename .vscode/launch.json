{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/raytracer.exe",
            "args": ["jsons/bvh_stress_test.json", "--bvh"],
            "stopAtEntry": true,
            "cwd": "${workspaceFolder}",
            "environment": []
        }
    ]
}
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/raytracer.exe",
            "args": ["jsons/binary_primitives.json"],
            "stopAtEntry": true,
            "cwd": "${workspaceFolder}",
            "environment": []
        }
    ]
}